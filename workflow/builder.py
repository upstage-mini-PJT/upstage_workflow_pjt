"""
메인 워크플로 그래프. main.py에서 invoke.
노드는 각 에이전트의 nodes 모듈에서 import 해서 add_node.
"""
# from langgraph.graph import StateGraph, END
# from agents.onboarding_agent.nodes import dummy_node  # 예: onboarding 노드

# builder = StateGraph(...)
# builder.add_node("onboarding_analyze", dummy_node)
# ...
# graph = builder.compile()